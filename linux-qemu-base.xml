<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- Generic manifests for linux and qemu /!-->
  <remote  name="origin"
           fetch=".." />

  <default revision="master"
           remote="origin"
           sync-j="2" />

  <!-- Specific projects /!-->
  <project path="linux" name="linux" />
  <project path="qemu"  name="qemu" />

</manifest>
